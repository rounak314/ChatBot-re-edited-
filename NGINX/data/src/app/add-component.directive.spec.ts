import { AddComponentDirective } from './add-component.directive';

describe('AddComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new AddComponentDirective();
    expect(directive).toBeTruthy();
  });
});
